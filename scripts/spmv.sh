#!/bin/bash
#SBATCH -N 1
#SBATCH --mem 120000
#SBATCH -p lanka-v3
#SBATCH --nodelist=lanka26
#SBATCH --exclusive

set -u

# out=/Users/danieldonenfeld/Developer/taco-compression-benchmarks/OUT_TEST/
out=/data/scratch/danielbd/out_spmv/
mkdir -p "$out"

BENCH=spmv BENCH_KIND=DENSE DATA_SOURCE=covtype OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
BENCH=spmv BENCH_KIND=SPARSE DATA_SOURCE=covtype OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
BENCH=spmv BENCH_KIND=RLE DATA_SOURCE=covtype OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench

BENCH=spmv BENCH_KIND=DENSE DATA_SOURCE=mnist OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
BENCH=spmv BENCH_KIND=SPARSE DATA_SOURCE=mnist OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
BENCH=spmv BENCH_KIND=RLE DATA_SOURCE=mnist OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench

BENCH=spmv BENCH_KIND=DENSE DATA_SOURCE=sketches OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
BENCH=spmv BENCH_KIND=SPARSE DATA_SOURCE=sketches OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
BENCH=spmv BENCH_KIND=RLE DATA_SOURCE=sketches OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench

BENCH=spmv BENCH_KIND=DENSE DATA_SOURCE=ilsvrc OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
BENCH=spmv BENCH_KIND=SPARSE DATA_SOURCE=ilsvrc OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
BENCH=spmv BENCH_KIND=RLE DATA_SOURCE=ilsvrc OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench


# CACHE=WARM BENCH=spmv BENCH_KIND=DENSE DATA_SOURCE=covtype OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
# CACHE=WARM BENCH=spmv BENCH_KIND=SPARSE DATA_SOURCE=covtype OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
# CACHE=WARM BENCH=spmv BENCH_KIND=RLE DATA_SOURCE=covtype OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench

# CACHE=WARM BENCH=spmv BENCH_KIND=DENSE DATA_SOURCE=mnist OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
# CACHE=WARM BENCH=spmv BENCH_KIND=SPARSE DATA_SOURCE=mnist OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
# CACHE=WARM BENCH=spmv BENCH_KIND=RLE DATA_SOURCE=mnist OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench

# CACHE=WARM BENCH=spmv BENCH_KIND=DENSE DATA_SOURCE=sketches OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
# CACHE=WARM BENCH=spmv BENCH_KIND=SPARSE DATA_SOURCE=sketches OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
# CACHE=WARM BENCH=spmv BENCH_KIND=RLE DATA_SOURCE=sketches OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench

# CACHE=WARM BENCH=spmv BENCH_KIND=DENSE DATA_SOURCE=ilsvrc OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
# CACHE=WARM BENCH=spmv BENCH_KIND=SPARSE DATA_SOURCE=ilsvrc OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
# CACHE=WARM BENCH=spmv BENCH_KIND=RLE DATA_SOURCE=ilsvrc OUTPUT_PATH=$out CACHE_KERNELS=0 make taco-bench
